# SYSTEM.LT - Recoge la base de datos del sistema completo, para convertir a LAMMPS usar moltemplate.sh -vmd sistema.lt

import polimero.lt

polymer = new polimero

write_once("Data Boundary") { 
   0.0 50.0 xlo  xhi 
   0.0 20.0 ylo  yhi 
   0.0 20.0 zlo  zhi 
} 
